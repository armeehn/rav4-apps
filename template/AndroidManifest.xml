<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rav4apps.template">
    <!-- Match the head unit: Android 13 (API 33). minSdk 28 mirrors the OEM apps. -->
    <uses-sdk android:minSdkVersion="28" android:targetSdkVersion="33"/>
    <!-- The launcher's theme provider. Without this line an API 30+ app cannot see it at
         all and silently keeps its own colours (see apps/_design Palette). -->
    <queries>
        <provider android:authorities="com.ripostelabs.carlauncher.theme" />
    </queries>
    <application android:label="@string/app_name" android:allowBackup="false">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
